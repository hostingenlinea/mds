// ... (Modelos anteriores Pastor e Iglesia)

model Acta {
  id            Int      @id @default(autoincrement())
  titulo        String
  contenido     String   @db.Text // Texto largo
  iglesiaNombre String
  fecha         DateTime @default(now())
}